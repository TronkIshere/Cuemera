# Known Limitations & Upgrade Roadmap

All P0 and P1 hygiene items are resolved. A sixth session fixed four additional device-confirmed bugs (front/back camera rotation, a native crash in Track 2, an EXIF-orientation bug, and an Android release-build failure) — see `README.md` §3a/§4/§6 for the account of each. **A seventh session started adding `sherpa_onnx` (VITS/Piper) as a second, primary TTS engine — this work is incomplete as of this writing** (asset wiring not finished, nothing device-verified yet); see `README.md` §8 and item 1 below. **A tenth session fixed five bugs found via debug logging on a real device** — a coaching pick-worst tie-break bias, a circular-angle deviation bug affecting both spoken coaching and auto-capture readiness, an inverted tolerance-slider UI, an AI-coaching timeout lockout, and a missing landmark-confidence gate on the reference-photo side — see `README.md` §11 for the full account. None of the tenth session's fixes have been device-verified beyond the debug-log evidence that diagnosed them; see the new items below. **An eleventh session unified pose-landmark trust filtering across reference-photo and live-camera paths (`core/pose/landmark_gate.dart`), applied the tenth session's likelihood gate to the live side for the first time, implemented the screenshot-extrapolation fix (Phases 0–2 of `REFERENCE_TRUST_FILTER_SOLUTION.md`), and found + fixed a real bug in the new Phase 2 crop-redetect code via an actual test screenshot** — see `README.md` §12. **A twelfth session found the eleventh session's own painter fix was itself still wrong and corrected it, rewrote Phase 2's crop-redetect logic to evidence-based reconciliation with veto power, enabled `PoseDetectionModel.accurate` for the reference-photo detector, and finished `sherpa_onnx`'s asset wiring — see `README.md` §13.** **A thirteenth session closed the AI-coaching investigation (real cause: `aiUnavailable` was tracked but never shown in Settings — see item 1, now resolved and removed from this list), then implemented an architecture audit's migration steps 1–4 and 8–10, fixing two more device-affecting bugs the audit didn't know to look for (`pose_analyzer.dart` never computing three of its five pose fields; the circular-angle bug from §11 recurring in `tracking_engine.dart`'s smoothing and three more evaluators) — see `README.md` §14.** **A fourteenth session finished the audit's migration plan entirely (LLM validation, `ModelLifecycleManager` wiring, both `EligibilityContext` stubs, `_evaluateFaceYaw`), plus fixed the `PoseLandmarkGate` `maskSignal` footgun, the `estimateSymmetry` shoulder-tilt bug in two files, a dropped-mid-utterance instruction, and unbounded LLM cadence — see `README.md` §15.** **A fifteenth session took all of that onto a physical device for the first time and found four more real bugs in the process: `sherpa_onnx` had been silently broken since the seventh session by a Flutter SDK breaking change (`AssetManifest.json` removal), a race condition in when it initializes, a bare `catch` hiding both, and a fourth-and-fifth recurrence of the circular-angle bug class in two files (`tracking_engine.dart`, `auto_capture_service.dart`) never reviewed before that session — see `README.md` §16.** Several items below are resolved or superseded accordingly; a few are now device-confirmed for the first time, called out explicitly where that's true.

## 1. Needs physical-device testing

- **Re-confirm all five tenth-session fixes on a real device with a real reference photo.** The `_pickWorst`/circular-deviation/tolerance-slider/AI-timeout/likelihood-gate fixes (`README.md` §11) were diagnosed from debug-log evidence and applied by inspection — none have been run through a full live session yet. Specifically confirm: coaching cues now vary across attributes instead of fixating on shoulder; `trackingProgress` can now actually reach `minTrackingProgressForCapture` with a genuinely-matched pose; the four "strictness" sliders now behave as their labels suggest; AI-generated phrases are actually heard at least sometimes in a normal session (not just confirmed reachable via `ai_gate:` logs); and a reference photo with a low-confidence shoulder landmark now correctly yields `null` there instead of a bogus angle.
- ~~**Apply the Bug 5 landmark-likelihood gate to `pose_analyzer.dart` (live camera side).**~~ **Done (eleventh session).** `pose_analyzer.dart` now runs the same `PoseLandmarkGate` the reference-photo side uses, plus the same hip-to-ankle `bodyRatio` extrapolation guard — see `README.md` §12. **Needs device verification**, same as everything else in that session: confirm a real low-confidence live shoulder detection now correctly yields `null` there instead of a bogus angle.
- **Device-verify the twelfth session's crop-redetect reconciliation rewrite and the corrected painter fix.** Phase 2 (`reference_photo_crop_redetect.dart`) no longer only recovers missing landmarks — it can now reject an original landmark when mask confidence at its own coordinates and the crop re-detection both strongly contradict it (`README.md` §13 has the full account, including the real red-shirt-photo log that motivated the rewrite). Separately, the painter (`reference_picker_sheet.dart`) had its own still-broken `PoseLandmarkGate.fromPoints()` call removed — it now renders the authoritative gate's `poseLandmarkPoints` directly with no second trust decision. Both changes are reasoned through against real log evidence but neither has been re-run and confirmed on a physical device since the rewrite. Also still open from the eleventh session: Phase 2's trigger threshold (⅓ of tracked extremities untrusted) remains an untuned first guess.
- **Investigate possible non-determinism in `ReferenceImageAnalyzer.analyze()`.** Observed once: the same source photo, picked twice, produced two different outcomes ("No pose or face detected" vs. a partial skeleton). Not yet reproduced under controlled conditions (re-pick the identical file 2-3 times in a row and compare) — could be a genuine race condition in `referenceProfileProvider`'s handling of repeated picks, or could simply have been two different source files. **Eleventh session added `kDebugMode`-gated diagnostic logging** (a cheap file-size+checksum fingerprint plus a detection-outcome summary, logged on every `analyze()` call) specifically to make this easier to confirm next time it's reproduced — the investigation itself is still open.
- ~~**Get `camera_screen.dart`/`auto_capture_service.dart`/`camera_service.dart` into a session.**~~ **Partially done (fifteenth session).** `camera_screen.dart` and `auto_capture_service.dart` are now reviewed — the live-camera debug HUD is confirmed to be `DebugPerfOverlay` (frame timing only, no pose/face readout), and two new `kDebugMode`-gated logs (`mirror_check:`, `auto_capture_check:`) now exist specifically to make "why didn't auto-capture fire" diagnosable from a log — see `README.md` §16. **`camera_service.dart` is still not in any session** — `CameraService.capture()`'s controller lifecycle (see the now-superseded item further below) is described only from `FILE_REFERENCE.md`'s own prior entry, not from the current source.
- ~~**Device-test the `sherpa_onnx` TTS path now that asset wiring is believed complete.**~~ **Done (fifteenth session) — and the asset wiring was never the problem.** `SherpaTtsService.init()` now confirmed reaching `isReady == true` on a real device, generated audio confirmed playing through `audioplayers`, `AppTtsService`'s fallback confirmed working (it had been silently active the whole time, masking the underlying failure). The actual, root-caused failure had nothing to do with `pubspec.yaml`'s asset list: Flutter's own `AssetManifest.json` file was removed in a Flutter-tool release after this feature was originally written in the seventh session — `_ensureModelExtracted()` was still reading it directly, which threw `Unable to load asset: "AssetManifest.json"` on every attempt, silently swallowed by a bare `catch` in `app_tts_service.dart` that this session also fixed. See `README.md` §16 for the full three-cause account (the asset-manifest bug, a race condition in when `init()` starts relative to first use, and the swallowed exception that hid both from every prior session's log review).
- **New (fifteenth session) — `ModelLifecycleManager.ensureReady()`'s install timeout needs one more device pass.** A stall-based timeout (resets on every progress event, not on total elapsed time) replaced an initial flat 5-minute timeout that would have incorrectly killed a real, slow-but-successful ~304MB download this session's own device log captured. The stall-reset version has not itself been separately confirmed against a real stalled (not just slow) download — only reasoned through against the one real log available, which was a slow-but-successful case, not a genuinely-hung one.
- ~~**Resolve why Track 2 AI-coaching phrases still aren't heard despite a valid `HF_TOKEN`.**~~ **Done (thirteenth session) — root cause was not either of the two candidates this item named.** `ensureInstalled()`'s call site was never missing (it's in `ai_coaching_providers.dart`'s `_install()`, simply not reviewed in the session that raised the question), and `aiCoachingSettingsProvider.enabled` defaults `false` as expected with no surprises. The real cause: `AiCoachingSettings.aiUnavailable` was tracked but never displayed in `settings_screen.dart`, so a 3-consecutive-timeout trip (the tenth session's Bug 4) left a user stuck on fallback phrases with the toggle still showing "on" and nothing indicating why. Fixed with a warning + Retry action — see `README.md` §14. **Still needs device verification**: confirm the warning actually appears after a real timeout streak, and that Retry actually restores AI phrases rather than just clearing the warning text.
- **Re-confirm the front/back camera rotation fix.** `MlKitService.rotationFor()` was rewritten this session to apply the correct lens-direction-specific formula (previously the caller used a single, device-orientation-blind formula for both cameras — see `README.md` §3a). The original symptom (`shoulderAngle`/`faceAngle` reading `FAIL` on the front camera despite a matching pose) was only observed via a debug-overlay screenshot, not a repeatable test — re-run on a physical device with the front camera and confirm the debug overlay reads `OK` for a genuinely matched pose. Also worth checking: does the app lock portrait orientation? If not, test with the phone rotated too, since `deviceOrientation` now actually matters for the computed rotation in a way the old formula ignored entirely.
- **Test the Impeller-disable mitigation across more devices.** One device (model "7 ZS67") showed clearly worse frame pacing with Impeller (the default renderer) than with Skia (`--no-enable-impeller`) — matches known upstream issues, not app-specific. The recommended fix (add `<meta-data android:name="io.flutter.embedding.android.EnableImpeller" android:value="false" />` to `AndroidManifest.xml`) has **not been confirmed applied to the manifest yet** — check whether it's actually there, and if so, whether it resolves jank on other test devices too, since Impeller regressions are GPU/driver-specific and this device is only one data point.
- ~~**`CameraService.capture()`'s per-photo controller lifecycle needs a redesign decision.**~~ **Superseded.** `CameraService` was merged this session from two independently-opened `CameraController`s (one for the live ML-analysis stream, one for preview/capture) into a single shared controller, matching how CameraX is actually meant to be driven (Preview + ImageAnalysis + ImageCapture as one bound session). `capture()` now just stops/resumes that one controller's image stream around `takePicture()` rather than juggling separate controllers — there's no more per-photo controller setup/teardown to redesign. **Needs physical-device verification** (the design decision itself is now resolved, see above): confirm continuous autofocus now behaves correctly (this was the original symptom — an Oppo Reno 14 showed preview/capture visibly softer than the stock camera app and non-continuous-feeling AF, matching known CameraX behavior on devices reporting a `LIMITED` Camera2 hardware level), confirm the ML Kit overlay/coaching resumes normally after a capture (i.e. the internal stream-resume actually fires), confirm `switchLens()` still works including front-camera rotation correctness, and validate that `ResolutionPreset.high` (the preset now used for the merged controller, previously only used for preview) doesn't slow down ML Kit frame processing to an unacceptable degree — no resolution-negotiation code was added to compensate, since CameraX already handles this internally when multiple use cases share one session.
- **Measure the real wall-clock impact of `ReferenceImageAnalyzer.analyze()`'s concurrency.** Its five independent steps (pose, face, segmentation, decode, palette) now run via `Future.wait` instead of sequentially. Confirm on a physical device that this actually cuts latency — ML Kit's native platform-channel bindings may serialize these calls internally regardless of Dart-level concurrency, in which case the real-world win could be smaller than assumed.
- **Verify all four `*IsMirrored` direction flags in `reference_comparison_engine.dart` — `_faceRollDirectionIsMirrored`, `_shoulderBalanceDirectionIsMirrored`, `_bodyYawDirectionIsMirrored`, and the fourteenth session's new `_faceYawDirectionIsMirrored`.** All four default `false` and remain unverified on a physical device — this is the single oldest unresolved item in this document, open since it was first flagged for `_faceRollDirectionIsMirrored` alone. **Fifteenth session added logging support for this**: `camera_screen.dart`'s new `mirror_check:` debug log (see `README.md` §16) prints lens direction plus all four flags' underlying raw values every ~500ms. Procedure: nudge/tilt/turn to a known physical side with the front camera, note the sign; switch to the back camera (`_flipCamera`), repeat the identical physical pose, compare signs. A flag that flips sign between lenses for the same physical pose needs to become a function of `lensDirection` rather than a fixed constant — no session has attempted this rewrite, since it needs the device data first. `bodyYawEstimate` additionally relies on ML Kit's *experimental* shoulder z-depth, which Google's own docs call less accurate than x/y — worth confirming the raw value moves monotonically across a few test poses (facing camera, then turned ~30°/60° each way), not just its sign.
- ~~**Re-tune auto-capture thresholds after `AutoCaptureService`'s new gates.**~~ **Superseded (fifteenth session) — the real bug was more specific than "too strict."** A live device log showed the opposite direction of problem: auto-capture firing on far less than a full-body match, because `bodyRatio`'s reference target was `null` (the reference photo's own knees/ankles were outside the analyzed frame) and `trackingProgress()` silently drops a `null`-target criterion from its average rather than counting it. Separately, `trackingProgress()`/`AutoCaptureService` never scored `shoulderBalanceRatio`/`bodyYawEstimate` at all, and four of `AutoCaptureService`'s angle gates used linear instead of circular deviation. See `README.md` §16 for the full account. **Addendum, outside the normal per-session log:** `shoulderBalanceRatio`/`shoulderSpanRatio` scoring/gating — left out of the fifteenth session on purpose, pending `comparison_math.dart` — has since been added to both `trackingProgress()` and `AutoCaptureService`, reusing `comparison_math.dart`'s existing `thresholdForPoseRatio()`/`maxDeviationForPoseRatio` unchanged (no edit to that file was actually needed). Not yet device-verified. The general "confirm a real subject can satisfy all gates within `minTrackingProgressForCapture`" ask still applies, now against this further-corrected code.
- **Confirm `ReferenceComparisonEngine.evaluate()`'s tier selection actually surfaces composition/lighting coaching in a live session.** Previously composition/lighting were never spoken as long as pose & face had any mild issue; a round-robin fix shipped in a later session (see below), then was itself superseded in the thirteenth session by `action_plan.dart`'s severity-aware, confidence-weighted `pickAcrossTiers()` plus `root_cause_engine.dart`'s cluster collapsing. None of this has been confirmed on-device — run a real session with a deliberately-off reference (e.g. wrong background/lighting but otherwise fine pose) and confirm those cues actually get voiced in rotation, not just pose/face repeating, and separately confirm a torso-rotation-or-lean cluster (shoulderAngle/shoulderBalance/shoulderSpan/bodyYaw all off at once) now gets voiced as one collapsed correction instead of four separate ones.
- **Confirm `light_analyzer.dart`'s mask-index scale fix actually changes `backgroundClutterCount` sanely on a real device (ninth session, see `README.md` §10).** The old `_computeBackgroundVariance` read the segmentation mask at the image's own `(x, y)` with no scale correction to the mask's own (usually much smaller) resolution — effectively sampling near-random mask pixels. The fix (`_analyzeLumaPlane`'s `maskScaleX`/`maskScaleY`) is a straightforward correction, but the resulting values have never been observed on-device; since `backgroundClutterCount` feeds both `AutoCaptureService._backgroundOk` and the editorial score's background component, watch for auto-capture behaving differently than before this fix and re-tune `DetectionThresholds.defaultBackgroundClutterThreshold` if needed.
- **Measure whether `MlKitService.processImage()`'s newly-concurrent pose/face/segmentation calls (ninth session) actually reduce per-frame latency.** Same caveat as the `ReferenceImageAnalyzer.analyze()` entry above — ML Kit's native platform-channel bindings may serialize these regardless of Dart-level concurrency. Compare `LightAnalyzer.debugLogFrameTiming` (or a similar timing log added around `processImage`) before/after on a real device before assuming this helped.
- **Confirm the ninth-session provider/rebuild-scope fixes (`README.md` §10) didn't silently change UI behavior.** `targetSubjectProfileProvider` no longer watches `subjectProfileProvider` (was unused, dead weight) and `currentScoreProvider` is now watched inside a local `Consumer` around just `ScoreBadge` instead of at `_buildReadyBody`'s top level — both should be behavior-neutral by inspection, but neither has been run on a device since the change.

## 2. Needs test coverage / code audit

- **`reference_image_analyzer.dart`'s `analyze()` orchestration is still untested.** It constructs real `PoseDetector`/`FaceDetector`/`SelfieSegmenter` instances internally and calls their native `processImage()` directly — there's no seam to inject a fake, so exercising `analyze()` itself needs a physical device/platform channel, not a pure-Dart test. `face_analyzer_test.dart` now covers `face_analyzer.dart` fully (`Face`'s public constructor makes it directly testable), and `reference_image_analyzer_test.dart` covers the four pure pixel/mask helpers (`estimateNegativeSpace`/`estimateSymmetry`/`estimateBackgroundClutter`/`estimateBrightness`, made public + `@visibleForTesting` for exactly this purpose) — but the pose/face detection calls and the `bodyRatio`/`poseLandmarkPoints` landmark logic inside `_analyzePose`/`_analyzeFace` remain uncovered. If `analyze()` itself needs to be under test, it likely needs a constructor-injected detector seam first. **Eleventh session made this larger, not smaller**: `_derivePose` is now async and can trigger a second `PoseDetector` pass plus temp-file I/O (`reference_photo_crop_redetect.dart`'s `reconcileWithCrop`) — none of that is under test either, and the same constructor-injected-detector-seam prerequisite applies to it too. `PoseLandmarkGate` and `sampleMaskTrust` themselves are pure functions (`core/pose/landmark_gate.dart`, `landmark_trust_classifier.dart`) and could be unit-tested independently of the detector seam question — not yet done. **Twelfth session's new `_verifyLandmark`/`LandmarkDecision` logic in `reference_photo_crop_redetect.dart` is also a pure function of its inputs** (original gate, crop landmarks, subject box) and is likewise a good unit-test candidate independent of the detector-seam question — not yet done either.
- **No unit tests yet for `_evaluateShoulderBalance`/`_evaluateShoulderSpan`/`_evaluateBodyYaw`** (eighth session, see `README.md` §9). Should mirror `reference_comparison_engine_test.dart`'s existing coverage for `_evaluateShoulderAngle`/`_evaluateFaceRoll` — null-field skip case, deviation-under-threshold no-fire case, mild/moderate/strong tier boundaries, and correct `direction` on both sides of the comparison for all three. **Same gap now applies to `_evaluateFaceYaw`** (fourteenth session, new).
- **`tracking_engine.dart`'s `trackingProgress()` and `auto_capture_service.dart` have no regression test locking down the fifteenth session's circular-deviation fixes.** `test/core/services/tracking_engine_test.dart` and `test/capture/services/auto_capture_service_test.dart` both exist (per `test/` in `FILE_REFERENCE.md`) but weren't run or updated this session — a wrap-boundary case (e.g. current=-178°, reference=170°) for `shoulderAngleDegrees`/`faceAngleDegrees`/`bodyYawEstimate` would be the highest-value addition, given this is the fourth/fifth confirmed recurrence of this exact bug class across the project.

## 3. Needs a product decision

- ~~**Decide a reset/retry policy for `coachingAiUnavailableProvider`.**~~ **Done (fourteenth session), device-confirmed (fifteenth session).** `model_lifecycle.dart`'s `ModelLifecycleManager` is now wired into `ai_coaching_providers.dart` as the shared source of truth across both listeners and the Settings toggle, with automatic capped-backoff retry (no manual action needed) plus a manual Retry that bypasses backoff. A real device session (fifteenth session, `README.md` §16) confirmed `lifecycleState=ready` and successful generation end-to-end, including surviving a slow-but-real ~304MB model download along the way.
- **Gate debug logging behind `kDebugMode` before a release build.** `ai_gate:`/`coaching_phrase_generation:` (`voice_providers.dart`) and `pick_worst:` (`reference_comparison_engine.dart`) are plain `debugPrint` calls, left ungated since the tenth session — unlike `reference_picker_sheet.dart`'s existing painter debug logging, which is already `kDebugMode`-gated. The twelfth session added a third ungated logger (`LANDMARK_VERIFY`/the crop-redetect summary line in `reference_photo_crop_redetect.dart`). **The fifteenth session's two new loggers in `camera_screen.dart` (`mirror_check:`, `auto_capture_check:`) are correctly `kDebugMode`-gated already** — worth using as the reference pattern when this item is finally addressed for the older, still-ungated loggers above.

- **Wire `ErrorReportingService` to a real remote sink.** It currently only retains the last 200 reports in memory (`FlutterError.onError`, `PlatformDispatcher.instance.onError`, `runZonedGuarded`, and every previously-silent `catch` all route through it) — reports don't survive app restart and never leave the device. A `TODO(remote-sink)` marks the extension point; needs a service chosen (Firebase Crashlytics, Sentry, custom backend) and an account/API keys before this can be wired up.
- **`enableClassification: false` on the live `FaceDetectorOptions` contradicts what this project's docs previously assumed** (`README.md` used to say classification was enabled downstream of Track 1's flag). As it stands, `eyesOpen`/`expression` are `null` for two independent reasons — the detector-level config *and* `FaceAnalyzer.enableEyeAndExpressionSignals = false` — meaning flipping the Track 1 flag back to `true` in the future would silently do nothing, since classification is already off one layer up. Needs a decision: either enable classification to match Track 1's original intent (re-enabling the eventual re-enable path), or update the docs/intent to reflect that classification is meant to stay off and remove the now-redundant `enableEyeAndExpressionSignals` flag.
- ~~**Harden `CoachingPhraseModelService._buildPrompt()` against future prompt-length growth.**~~ **Partially done (fourteenth session).** A conservative character-based ceiling (the cheap/approximate option this item named, not an actual tokenizer-based count) is now checked before every `generate()` call — an over-budget prompt is skipped rather than risking the native SIGABRT class of crash. Not device-confirmed to actually trigger correctly on an over-budget prompt (none was generated during the fifteenth session's real usage), and a real tokenizer-based count was deliberately not pursued, so this is the blunt version of the fix, not the precise one.
- **Type `voice_providers.dart`'s `emphasisFor()` parameter properly.** It's currently written with no type annotation on purpose (`emphasisFor(severityBand)`) because `coaching_decision.dart`'s actual `severityBand` return type wasn't available when this was written — the string-matched `.name` call works if it's a plain Dart enum, but this should be tightened to a real type once that file is confirmed, both for compile-time safety and to catch a future rename of the enum's values.

## 4. Model-driven upgrade — the core "complete product" gap

**Track 2 (Gemma 3 270M coaching phrases) is code-complete, LLM-output-validated, and now device-confirmed running end to end for the first time (fifteenth session).** Phases 0–2 are code-complete: the integration smoke test passed on a physical device (all 5 sample `CoachingDecision`s installed and generated successfully), a Settings toggle ("AI Coaching Phrases") controls activation end-to-end, `ModelLifecycleManager` provides shared, automatic-recovery model-availability state, and `LlmOutputValidator` checks every generated phrase before it's spoken. A real device session (fifteenth session, `README.md` §16) confirmed `lifecycleState=ready` and the native `llm_inference_engine.cc` session-config log firing after a real ~304MB download completed. See `README.md` §4/§15/§16 for the full account, including bugs found and fixed getting here: an unawaited `FlutterGemma.initialize()` race condition, an `assert`-based crash when `HF_TOKEN` is unset, a native `SIGABRT` when a coaching prompt exceeded a too-low `maxTokens` (fixed, then further hardened with a preventive character-budget guard — item 3 above), a silent `aiUnavailable` state with no UI, and — this round — `ModelLifecycleManager.ensureReady()` having no timeout at all around a hung/slow install.

**Remaining, in order:**
1. **Capture a full Phase 3 dataset.** Still only one of the 5 sample decisions' latency was ever captured in a log (`hue`/strong: 2200ms, from the original smoke test). Re-run and save the complete output — need all 5 latencies, plus real-device usage during an actual shoot, to judge whether `gemma-3-270m-it-q8` is the right size/quantization and whether phrasing is actually natural-sounding. The fifteenth session's real device pass generated at least one real coaching prompt (`[transformToChatPrompt] ... result length=759`) but the pasted log excerpt didn't capture whether/what it spoke or its latency.
2. **Getting the Hugging Face gated-model access right the first time is fiddly** — worth a one-line note in onboarding docs for future contributors: having a valid `HF_TOKEN` isn't sufficient on its own; the account behind that token must separately visit `litert-community/gemma-3-270m-it` on huggingface.co and accept the Gemma license, or every download attempt fails with an HTTP 401. **A related, newly-confirmed real-world wrinkle (fifteenth session):** even with the license accepted, a ~304MB download over HuggingFace's Xet CDN can appear to stall for a long stretch between progress updates while genuinely still downloading — worth documenting so a future session doesn't misdiagnose a slow-but-working download as broken, the way this session initially did before the full log came in.
3. Once (1)–(2) are done and the app's seen real use: judge phrase-quality/naturalness, and revisit whether the ~304MB download size is worth it for a 270M model vs. a smaller quantization if one becomes available.

**Toolchain note (this session):** getting `flutter_gemma`/`flutter_gemma_mediapipe` building on Android required bumping AGP (`8.7.3` → `8.11.1`), Gradle (`8.12` → `8.14.3`), Kotlin (`2.1.0` → `2.2.20`), and NDK (`27.0.12077973` → `28.2.13676358`, specifically required by `integration_test`). All are now current — see `README.md`'s Toolchain section. Worth knowing if this project sits untouched for a while and these plugins get further updated: the AAR-metadata-vs-AGP-version failure mode (`checkDebugAarMetadata`) is likely to recur with future dependency bumps and is a quick diagnosis (`./gradlew assembleDebug --stacktrace` — the real error is always further up than Flutter's wrapped exit-code message suggests).

**Toolchain note (sixth session) — resolved:** the above toolchain bumps got the app compiling, but `flutter build apk --release` separately failed at `:app:minifyReleaseWithR8` with two missing MediaPipe profiling classes. Root cause: `android/app/build.gradle.kts`'s `release` block had no `proguardFiles(...)` call at all, so ProGuard/R8 rules were never applied regardless of what existed in `proguard-rules.pro`. Fixed by adding `proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro")` and creating `proguard-rules.pro` with the two `-dontwarn` lines Gradle's generated `missing_rules.txt` specified. A release APK now builds (~230MB — `--split-per-abi` would shrink this considerably for real distribution, since the current build bundles all ABIs into one universal APK).

**Toolchain note (seventh session) — resolved, but see item 1 above for what's still incomplete:** while wiring up `sherpa_onnx`, `pubspec.yaml` was found with two structural bugs unrelated to sherpa_onnx itself but blocking it entirely: a top-level `assets:` key (Flutter silently ignores `assets:` unless it's nested under `flutter:` — meaning *no* assets, including the pre-existing `assets/images/`, were ever actually being bundled), and `flutter_launcher_icons.image_path` had `assets/models/vits-en/` mistakenly appended to what should be a single icon-file path. Both fixed. Separately noticed but **not fixed**: `environment.sdk: ^3.8.1` in this same file doesn't match the `^3.12.0` this doc's own Toolchain section above says was bumped to for Track 2 — worth resolving which one is actually true on the current dev machine before it causes confusion.

**Toolchain note (twelfth session):** no toolchain changes this session. The `pubspec.yaml` `assets:` list was completed (see `README.md` §13 and item 1 above) — a documentation/asset-declaration fix, not a toolchain version bump.

Two items from this section remain untouched and still open:
- **Fold the unified expression classifier (`expression_classifier.dart`) into that same modeling effort**, rather than continuing to hand-tune its probability thresholds. (Currently gated off the live path entirely by Track 1's signal-disable flag, which further reduces urgency here for now.)
- **Scope a Quality Engine** — aesthetic scoring beyond `ComparisonMath`'s geometric comparison. No ready-made on-device model exists for this; the realistic path is a NIMA-style small CNN (MobileNet/Inception backbone) fine-tuned and exported to LiteRT. Needs a labeled dataset first — bootstrap from a public aesthetic-scoring dataset, then fine-tune on real app data — before any model work starts.

## 5. Audit migration — what's still open (fifteenth session)

A standalone architecture audit produced an 11-step migration plan. Steps 1–10 are all now implemented (steps 5–7 and the eligibility-context half of step 10 closed out in the fourteenth session — see `README.md` §15); step 11 is intentionally not attempted. What's genuinely still open, after two rounds of device time (fourteenth session code, fifteenth session's first real device pass):

- ~~**Step 7 not done.**~~ **Done (fourteenth session), device-confirmed (fifteenth session).** `ModelLifecycleManager` is wired into `ai_coaching_providers.dart`, shared with both listeners, with automatic capped-backoff retry. Confirmed reaching `lifecycleState=ready` on a real device after a real model download. See item 3/§3 above.
- ~~**LLM output validation not wired anywhere.**~~ **Done (fourteenth session).** `llm_output_validator.dart`/`llm_contract.dart` are now called from both listeners before a generated phrase is ever spoken. Not yet confirmed on-device that a real generated phrase has actually failed validation and correctly fallen back — only the pass-through path (a valid phrase, spoken) has real device evidence so far.
- ~~**`CoachingEligibility`'s confidence floor isn't enforced on the original listener.**~~ **Done (fourteenth session).** `voice_providers.dart` now enforces `ConfidenceFloors.eligibleToSpeak` too, with real effect for the four pose attributes with wired confidence.
- ~~**`voiceDirectorListenerV2Provider`'s `EligibilityContext` is stubbed, not real.**~~ **Done (fourteenth session), but with a caveat on what "real" means.** `temporallyEligible` reads genuine per-metric stabilizer state (`SubjectProfile.metricTemporalEligibility`, from `StabilizedMetric.isEligible`). `subjectFullyInFrame`/`detectorsAgree` are real per-frame detector-presence checks (did the pose/face detector find something this frame), computed in `scene_providers.dart` — but this is a lighter-weight signal than true geometric "is the subject's whole body in frame" or "do the detectors geometrically agree," which the live path has no wiring for (the mask signal is deliberately never sampled on the live path, for per-frame cost reasons — a documented, intentional asymmetry vs. the reference-photo path, not an oversight). **Still correctly recommend not enabling "Coaching v2 (experimental)" for real users** until real device soak time exists — the stub is gone, but "real" here is presence-level, not geometric-level, and neither has run through an actual session yet.
- ~~**`_evaluateFaceYaw` still doesn't exist.**~~ **Done (fourteenth session).** Uses `faceAngleDegrees` (ML Kit's `headEulerAngleY`), a signal that turned out to already be captured and stored on both `SubjectProfile`/`ReferenceProfile` since the eighth session, just never read by any evaluator. Its direction flag (`_faceYawDirectionIsMirrored`) joins the still-open device-verification item above.
- **No tests for any of this — still true, unchanged across two more sessions.** The audit's own §22 test plan was never started, and the fourteenth/fifteenth sessions' fixes (LLM validation, model lifecycle, eligibility context, `_evaluateFaceYaw`, the `estimateSymmetry`/circular-deviation/`PoseLandmarkGate` bugs) added more surface area with no coverage, not less. `reference_comparison_engine_test.dart` needs updating for `confidence`/`controllability` and root-cause collapsing (unchanged from the thirteenth session's note) and now also for `_evaluateFaceYaw`. This was an explicit, repeated choice across both sessions, not an oversight.
- **Step 11 (flip the v2 flag, delete the old listener) intentionally still not attempted** — the two gaps that used to structurally block it (LLM validation, the eligibility-context stub) are both closed now, but it was always correctly sequenced last behind real device soak time too, which two sessions of live debugging on unrelated bugs doesn't substitute for.

**New, found only once real files got reviewed for the first time (not part of the original audit's own findings, since it never had these files either):**
- `PoseLandmarkGate`'s `maskSignal` silent-default footgun — fixed at the source (fourteenth session): all three factories require it now instead of defaulting to `MaskTrustSignal.none`.
- `estimateSymmetry` silently re-measuring shoulder tilt instead of symmetry, in both `reference_image_analyzer.dart` and `light_analyzer.dart` — fixed (fourteenth session).
- A dropped-mid-utterance coaching instruction on the original listener, and unbounded LLM-generation cadence tied directly to decision-change frequency — both fixed (fourteenth session).
- A fourth and fifth recurrence of the circular-quantity bug class (first found in the tenth session), this time in `tracking_engine.dart`'s `trackingProgress()` and `auto_capture_service.dart` — two files neither this audit nor any prior session had ever reviewed. Fixed for the angle-based checks (fifteenth session); `shoulderBalanceRatio`/`shoulderSpanRatio` scoring/gating was left out at the time, pending `comparison_math.dart`, but has since been added to both files (outside the normal per-session log — `comparison_math.dart` itself needed no changes). Not yet device-verified.